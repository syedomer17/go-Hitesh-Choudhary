package main 

// imports brings external packages into the file that u are working on. 
import ("fmt"
		"math"
		)

func main() {
	// package name followed by a dot and then the function name.
	fmt.Println("sqrt(25)",math.Sqrt(25))
}